import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lucasalencar.pythonnobolso',
  appName: 'Python no Bolso',
  webDir: 'www'
};

export default config;
